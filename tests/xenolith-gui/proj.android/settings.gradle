pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
include ':app'
include ':appsupport'
include ':firebase'
project(':appsupport').projectDir = new File('../../../xenolith/platform/android/appsupport')
project(':firebase').projectDir = new File('../../../xenolith/platform/android/firebase')
